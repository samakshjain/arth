services:
  webserver:
    build:
      target: production
    restart: always
    healthcheck:
      test: ['CMD', 'wget', '--quiet', '--tries=1', '--spider', 'http://localhost:4321']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  typesense:
    restart: always
